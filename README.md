# Richard's dotfiles for Linux (Ubuntu/Mint)

## Installation

First install ZSH:

    ./install_zsh.sh

Then, or when ZSH is installed already, install the dotfiles:


    ./install_dotfiles.zsh

That's all!
